REMEDIATION PROMPT — paste entire file into Lovable as one message.

Target: Gather / community-events-hub.

PROBLEM
FSP text describes “Host dashboard” with upcoming/past event lists and stats. Current /dashboard lists host orgs (“Your hosts”), while per-event stats live under /h/$slug/manage—confusing versus written requirements.

TASK
Choose minimal UX clarity: either (A) rename navigation labels—“Dashboard” → “My hosts” and add subtitle pointing to Manage for event stats—or (B) add an “Events” tab/section under each host card showing upcoming vs past split with Going/Waitlist/Checked-in without duplicating entire manage UI. Prefer smallest diff that aligns copy with reality.

ACCEPTANCE
First-time hosts understand where to find event lists and RSVP stats relative to FSP wording.
